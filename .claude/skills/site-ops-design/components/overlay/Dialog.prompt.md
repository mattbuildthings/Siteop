Modal confirmation/detail overlay, centered.

```jsx
<Dialog title="Delete note?" footer={<><Button variant="ghost">Cancel</Button><Button variant="danger">Delete</Button></>}>This can't be undone.</Dialog>
```
